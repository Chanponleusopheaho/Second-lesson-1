#include <stdio.h>
#include <math.h>
int main2(){ //if we want to run chnage it to main()and the file not run change it to main1()
  float x;
  x = (float)((2*7.0)-5);
  float y;
  y =(float)(7.0*(9.0+10));
  float z;
  z=sqrt(x/y);
  printf("%f",z);
  return 0;
}